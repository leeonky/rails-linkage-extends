RSpec.describe Rails::Linkage::Extends do
  it "has a version number" do
    expect(Rails::Linkage::Extends::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
